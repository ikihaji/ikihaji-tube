{
  "name": "@ikihaji-tube/scrobble-chrome-extension",
  "version": "0.1.0",
  "repository": "https://github.com/ikihaji/ikihaji-tube.git",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch run ./src/index.ts",
    "build": "bun build ./src/*.ts --minify --outdir ./dist/",
    "start": "./dist/scrobble-chrome-extension",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "test": "bun test"
  },
  "dependencies": {
    "@elysiajs/eden": "1.1.3",
    "@ikihaji-tube/api": "workspace:*",
    "@ikihaji-tube/core": "workspace:*",
    "ts-pattern": "5.5.0"
  },
  "devDependencies": {
    "@ikihaji-tube/tsconfig": "workspace:*",
    "@types/chrome": "0.0.279"
  }
}
