{
  "name": "@ikihaji-tube/scrobble-client",
  "version": "0.1.0",
  "repository": "https://github.com/ikihaji/ikihaji-tube.git",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "bun --watch run ./src/index.ts",
    "build": "bun build ./src/index.ts --compile --minify --outfile ./dist/scrobble-client",
    "start": "./dist/scrobble-client",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "test": "bun test"
  },
  "dependencies": {
    "@elysiajs/eden": "1.1.3",
    "@ikihaji-tube/api": "workspace:*",
    "@ikihaji-tube/core": "workspace:*"
  },
  "devDependencies": {
    "@ikihaji-tube/tsconfig": "workspace:*"
  }
}
